# Calcuta

Текстовый редактор с живой математикой в стилистике редактора кода — вдохновлён [numbr.dev](https://numbr.dev), с рядом доработок. Тёмная тема, минималистичный интерфейс.

Всё приложение — один самодостаточный файл `index.html` (HTML + CSS + JS, без сборки и зависимостей). Просто открой его в браузере.

## Возможности

| Что | Как |
|---|---|
| **Живой расчёт** | Любое выражение считается на лету, результат — в правой колонке. Клик по результату копирует чистое число. |
| **Математика** | `+ - * / ^ %`, скобки, суффиксы `k`/`M`, проценты (`100 + 25%`, `25% of 100`). Слова-контекст вокруг чисел игнорируются. |
| **Переменные** | `бюджет = 500k` — подсветка фиолетовым, максимальный приоритет. Используются дальше по тексту. |
| **Агрегация** | `sum` / `total` (сумма секции), `avg` (среднее), `prev` (предыдущий результат). Секцию сбрасывает надзадача `//` или заголовок `#`. |
| **Чекбоксы** | Команда `++ ` превращает строку в чекбокс. Клик по галочке → зелёный фон, бледный текст. |
| **Надзадачи** | Строка с `//` — надзадача, подсвечена голубым. Обычные строки (подзадачи) — белые. |
| **Тэги** | Задаются в боковом меню (текст + цвет). Подсветка тэга приоритетнее всего, кроме переменных. |
| **Даты** | Формат `дд.мм` подсвечивается жёлтым. |
| **Числа** | Группировка тысяч (визуальная, тонкий пробел), десятичный разделитель — запятая. При копировании пробелы убираются; при вставке число нормализуется под формат. |
| **Номера строк** | Тонкий столбец слева. Выделяй номера (клик, Shift, Ctrl) и задавай им цвет через правый клик. |
| **Наведение** | Строка под курсором слегка светлеет. Наведение на переменную/операцию подсвечивает её исходную строку. |
| **Автосохранение** | Текст, тэги и цвета строк хранятся в `localStorage`. |

## Приоритет подсветки

`Переменная (фиолетовый) → Тэг → Надзадача // (голубой) → Дата (жёлтый) → Число`

## Синтаксис — коротко

```
// Бюджет проекта
доход = 2 500 000
налог = доход * 13%
чистыми = доход - налог
[ ] согласовать смету
[x] отправить отчёт 15.09
sum
```

## Заметки о формате чисел

- Десятичный разделитель — **запятая** (`12,5`). Точка в `дд.мм` трактуется как **дата** — это снимает конфликт «дата vs дробь».
- Число, введённое подряд (`2500000`), автоматически группируется тонким пробосом (`2 500 000`). Пробелы визуальные — копирование даёт `2500000`.

## Разработка / тесты

Логика движка вынесена в `window.__calcuta` (`analyze`, `evaluate`, `fmt`, `regroupText`, …), что позволяет юнит-тестировать её из headless-браузера без UI.
